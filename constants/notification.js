const NOTIFICATION_STATUS = {
    READ: "read",
    UNREAD: "unread"
}

const NOTIFICATION_TYPES = {
    INDIVIDUAL: "individual",
    CUSTOM: "custom",
    GLOBAL: "global"
}

module.exports = {
    NOTIFICATION_STATUS,
    NOTIFICATION_TYPES
}