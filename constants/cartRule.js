"use strict";

const CART_RULE_TYPE = {
  MERCHANT: "merchant",
  NETWORK: "network",
};

const CART_RULE_ACTION_TYPE = {
  PERCENTAGE: "percentage",
  FIXED: "fixed",
};

const CART_RULE_SOURCE = {
  BRAND: "brand",
  ADMIN: "admin",
};

module.exports = {
  CART_RULE_TYPE,
  CART_RULE_ACTION_TYPE,
  CART_RULE_SOURCE,
};
